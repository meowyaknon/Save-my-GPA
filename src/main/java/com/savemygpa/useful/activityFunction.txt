private void perform(Activity activity) {

    if (!activity.canPerform(player, timeSystem)) {
        showPopup("You cannot perform this activity.");
    }

    activity.performActivity(player,timeSystem);

    if(timeSystem.isDayOver()){
        timeSystem.endDay();
    }

    updateStats();
}
